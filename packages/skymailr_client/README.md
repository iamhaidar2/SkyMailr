Install in editable mode from the monorepo:

`pip install -e packages/skymailr_client`

Environment:

- `SKYMAILR_BASE_URL` — e.g. `https://your-skymailr.up.railway.app`
- `SKYMAILR_API_KEY` — tenant API key (`sk_live_...`)

See the main SkyMailr README for API details.
